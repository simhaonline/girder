---
version: "3"
services:
  mongodb:
    image: mongo:4.2
    ports:
      - 27017
    volumes:
      - /data/db

  girder:
    build: .
    ports:
      - 8080:8080
    command: girder serve --database mongodb://mongodb:27017/girder
